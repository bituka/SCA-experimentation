<section>
    <h1>Reps List Template: {{hellogoryo}}</h1>

    {{#if hasReps}}
        <div data-view="Rep.List"></div>tt

    {{else}}
        <p class="case-list-empty">{{translate 'No reps were found'}}</p>

    {{/if}}
</section>